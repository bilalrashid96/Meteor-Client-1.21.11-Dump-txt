package meteordevelopment.meteorclient.systems.accounts;

public enum AccountType {
   Cracked,
   Microsoft,
   TheAltening,
   Session;

   // $FF: synthetic method
   private static AccountType[] $values() {
      return new AccountType[]{Cracked, Microsoft, TheAltening, Session};
   }
}
